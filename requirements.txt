# -----------------------------------------------------------------------
# requirements.txt para deploy no Vercel
# Redis e FlareSolverr foram removidos; cache usa memória nativa.
# -----------------------------------------------------------------------

# Web framework (escolha conforme o projeto usa)
fastapi>=0.111.0
# flask>=3.0.0  ← descomente se o projeto for Flask

# ASGI adapter para Vercel + FastAPI
mangum>=0.19.0

# HTTP client assíncrono
httpx>=0.27.0
aiohttp>=3.9.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Dados / parsing
python-dateutil>=2.9.0

# Outros utilitários comuns no projeto
uvicorn>=0.30.0       # só para rodar localmente; Vercel usa Mangum
pydantic>=2.7.0

# Tracker UDP scraping (pode falhar em Vercel por restrições UDP — remova se der erro)
# aiobtdht>=0.0.5

flask>=3.0.0
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
python-dateutil>=2.9.0
httpx>=0.27.0
aiohttp>=3.9.0
